<h2>Platser från kategorin <%= @category['name'].capitalize %></h2>
    
    <%= @places.each do |places| %>
        <li>
            <a href="/places/<%= place['id'] %>"> <%= place['name'] %> </a>
        </li>
    <% end %>

<h5><a href="/places">Tillbaka till startsidan</a></h5>